spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

  devtools:
    restart:
      enabled: true
      poll-interval: 2s
      quiet-period: 1s

    livereload:
      enabled: true